{
  "name": "foxyclient",
  "version": "1.0.0",
  "description": "Foxy Server",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adam Popławski",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.10",
    "fs": "^0.0.1-security",
    "tslint": "^6.1.3",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "axios": "^0.24.0"
  }
}
